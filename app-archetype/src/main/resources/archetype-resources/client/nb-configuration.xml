<?xml version="1.0" encoding="UTF-8"?>
<project-shared-configuration>
    <!--
This file contains additional configuration written by modules in the NetBeans IDE.
The configuration is intended to be shared among all the users of project and
therefore it is assumed to be part of version control checkout.
Without this configuration present, some functionality in the IDE may be limited or fail altogether.
-->
    <formeditor xmlns="http://controlsjs.com/netbeans/tools/formeditor/">
        <formeditor_sourcefilter>"system;custom;#if($skin_winxp == "1")winxp%20skin;#end#if($skin_wineight == "1")wineight%20skin;#end#if($skin_wireframe == "1")wireframe%20skin;#end"</formeditor_sourcefilter>
    </formeditor>
</project-shared-configuration>
